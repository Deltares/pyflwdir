name: pyflwdir-test

channels:
  - conda-forge

# note that these are minimal packages required for testing purposes
dependencies:
  # required
  - python>=3.7
  - affine
  - numba>=0.54
  - numpy
  - scipy
  # test
  - black
  - pytest
  - pytest-cov
